<style>
    .hero-section {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 80px 20px;
        min-height: 70vh;
    }

    .hero-logo {
        font-size: 5rem;
        background: linear-gradient(45deg, var(--neon-cyan), var(--neon-purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 20px;
        filter: drop-shadow(0 0 15px rgba(0, 242, 255, 0.3));
    }

    .hero-title {
        font-size: 4rem;
        font-weight: 700;
        margin-bottom: 15px;
        line-height: 1.1;
    }

    .hero-subtitle {
        font-size: 1.25rem;
        color: var(--text-muted);
        max-width: 600px;
        margin-bottom: 40px;
        line-height: 1.6;
    }

    .hero-buttons {
        display: flex;
        gap: 20px;
    }

    .stat-bar {
        display: flex;
        gap: 40px;
        margin-top: 80px;
        padding: 20px 40px;
        background: var(--glass-bg);
        border-radius: 100px;
        border: 1px solid var(--glass-border);
    }

    .stat-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .stat-value {
        font-weight: 700;
        color: var(--neon-cyan);
        font-size: 1.2rem;
    }

    .stat-label {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: var(--text-muted);
    }
</style>

<div class="hero-section">
    <div class="hero-logo"><i class="fas fa-meteor"></i></div>
    <h1 class="hero-title">Orion's Restaurant <br> Management</h1>
    <p class="hero-subtitle">
        Optimiza tu servicio con nuestro sistema de gestión de órdenes de última generación. 
        Precisión, velocidad y control total en un solo lugar.
    </p>

    <div class="hero-buttons">
        <a href="/tasks/list" class="btn-neon" style="padding: 15px 40px;">
            Ver Órdenes <i class="fas fa-list-ul"></i>
        </a>
        <a href="/tasks/create" class="btn-neon" style="border-color: var(--neon-purple); color: var(--neon-purple);">
            Nueva Orden <i class="fas fa-plus"></i>
        </a>
    </div>

    <div class="stat-bar">
        <div class="stat-item">
            <span class="stat-value">Cocina</span>
            <span class="stat-label">Modulo 1</span>
        </div>
        <div class="stat-item" style="border-left: 1px solid var(--glass-border); padding-left: 40px;">
            <span class="stat-value">Mesas</span>
            <span class="stat-label">Modulo 2</span>
        </div>
        <div class="stat-item" style="border-left: 1px solid var(--glass-border); padding-left: 40px;">
            <span class="stat-value">Caja</span>
            <span class="stat-label">Modulo 3</span>
        </div>
    </div>
</div>